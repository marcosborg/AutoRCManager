@extends('layouts.admin')
@section('content')
<div class="content">@can('part_receipt_create')<p><a class="btn btn-success" href="{{ route('admin.part-receipts.create') }}">Nova receção</a></p>@endcan<div class="panel panel-default"><div class="panel-heading">Receções de Peças</div><div class="panel-body"><table class="table table-bordered table-striped"><thead><tr><th>ID</th><th>Encomenda</th><th>Recebido em</th><th>Local</th><th>Recebido por</th><th></th></tr></thead><tbody>@foreach($partReceipts as $receipt)<tr><td>#{{ $receipt->id }}</td><td>#{{ $receipt->part_order_id }}</td><td>{{ optional($receipt->received_at)->format('Y-m-d H:i') }}</td><td>{{ $receipt->received_location }}</td><td>{{ $receipt->received_by->name ?? '-' }}</td><td><a class="btn btn-xs btn-primary" href="{{ route('admin.part-receipts.show', $receipt) }}">{{ trans('global.view') }}</a> <a class="btn btn-xs btn-info" href="{{ route('admin.part-receipts.edit', $receipt) }}">{{ trans('global.edit') }}</a></td></tr>@endforeach</tbody></table>{{ $partReceipts->links() }}</div></div></div>
@endsection
